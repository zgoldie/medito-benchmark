workflows:
  ios-build-m2:
    name: iOS Build (M2)
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      flutter: 3.41.6
      xcode: 26.3
      groups:
        - firebase_config
    cache:
      cache_paths:
        - ~/.pub-cache
    scripts:
      - name: Set up Firebase config files
        script: |
          if [ -n "$FIREBASE_OPTIONS_DART" ]; then
            echo "$FIREBASE_OPTIONS_DART" > lib/firebase_options.dart
          else
            echo "FIREBASE_OPTIONS_DART not set; using committed fallback firebase_options.dart"
          fi
          if [ -n "$GOOGLE_SERVICE_INFO_PLIST" ]; then
            echo "$GOOGLE_SERVICE_INFO_PLIST" > ios/Runner/GoogleService-Info.plist
          else
            echo "GOOGLE_SERVICE_INFO_PLIST not set; skipping GoogleService-Info.plist override"
          fi
      - name: Get Flutter packages
        script: flutter pub get
      - name: Generate Pigeon code
        script: flutter pub run pigeon --input pigeon_conf.dart --package_name=medito
      - name: Generate Riverpod code
        script: dart run build_runner build --delete-conflicting-outputs
      - name: Build iOS (release, no codesign)
        script: |
          flutter build ios --release --no-codesign \
            --dart-define=APP_KEY=benchmark \
            --dart-define=ENVIRONMENT=prod \
            --dart-define=CONTENT_BASE_URL=https://example.com \
            --dart-define=AUTH_URL=https://example.com \
            --dart-define=EDIT_STATS_URL=https://example.com \
            --dart-define=DONATION_BASE_URL=https://example.com \
            --dart-define=DONATION_TOKEN=benchmark \
            --dart-define=SUPERWALL_API_KEY=benchmark \
            --dart-define=PAYWALL_ENV=live \
            --dart-define=FACEBOOK_APP_ID=benchmark \
            --dart-define=FACEBOOK_CLIENT_TOKEN=benchmark \
            --dart-define=ENABLE_DEVICE_PREVIEW=false
    artifacts:
      - build/ios/iphoneos/*.app

  ios-build-m4:
    name: iOS Build (M4)
    max_build_duration: 30
    instance_type: mac_mini_m4
    environment:
      flutter: 3.41.6
      xcode: 26.3
      groups:
        - firebase_config
    cache:
      cache_paths:
        - ~/.pub-cache
    scripts:
      - name: Set up Firebase config files
        script: |
          if [ -n "$FIREBASE_OPTIONS_DART" ]; then
            echo "$FIREBASE_OPTIONS_DART" > lib/firebase_options.dart
          else
            echo "FIREBASE_OPTIONS_DART not set; using committed fallback firebase_options.dart"
          fi
          if [ -n "$GOOGLE_SERVICE_INFO_PLIST" ]; then
            echo "$GOOGLE_SERVICE_INFO_PLIST" > ios/Runner/GoogleService-Info.plist
          else
            echo "GOOGLE_SERVICE_INFO_PLIST not set; skipping GoogleService-Info.plist override"
          fi
      - name: Get Flutter packages
        script: flutter pub get
      - name: Generate Pigeon code
        script: flutter pub run pigeon --input pigeon_conf.dart --package_name=medito
      - name: Generate Riverpod code
        script: dart run build_runner build --delete-conflicting-outputs
      - name: Build iOS (release, no codesign)
        script: |
          flutter build ios --release --no-codesign \
            --dart-define=APP_KEY=benchmark \
            --dart-define=ENVIRONMENT=prod \
            --dart-define=CONTENT_BASE_URL=https://example.com \
            --dart-define=AUTH_URL=https://example.com \
            --dart-define=EDIT_STATS_URL=https://example.com \
            --dart-define=DONATION_BASE_URL=https://example.com \
            --dart-define=DONATION_TOKEN=benchmark \
            --dart-define=SUPERWALL_API_KEY=benchmark \
            --dart-define=PAYWALL_ENV=live \
            --dart-define=FACEBOOK_APP_ID=benchmark \
            --dart-define=FACEBOOK_CLIENT_TOKEN=benchmark \
            --dart-define=ENABLE_DEVICE_PREVIEW=false
    artifacts:
      - build/ios/iphoneos/*.app

  ios-build-m4-max:
    name: iOS Build (M4 Max)
    max_build_duration: 30
    instance_type: mac_studio_m4_max
    environment:
      flutter: 3.41.6
      xcode: 26.3
      groups:
        - firebase_config
    cache:
      cache_paths:
        - ~/.pub-cache
    scripts:
      - name: Set up Firebase config files
        script: |
          if [ -n "$FIREBASE_OPTIONS_DART" ]; then
            echo "$FIREBASE_OPTIONS_DART" > lib/firebase_options.dart
          else
            echo "FIREBASE_OPTIONS_DART not set; using committed fallback firebase_options.dart"
          fi
          if [ -n "$GOOGLE_SERVICE_INFO_PLIST" ]; then
            echo "$GOOGLE_SERVICE_INFO_PLIST" > ios/Runner/GoogleService-Info.plist
          else
            echo "GOOGLE_SERVICE_INFO_PLIST not set; skipping GoogleService-Info.plist override"
          fi
      - name: Get Flutter packages
        script: flutter pub get
      - name: Generate Pigeon code
        script: flutter pub run pigeon --input pigeon_conf.dart --package_name=medito
      - name: Generate Riverpod code
        script: dart run build_runner build --delete-conflicting-outputs
      - name: Build iOS (release, no codesign)
        script: |
          flutter build ios --release --no-codesign \
            --dart-define=APP_KEY=benchmark \
            --dart-define=ENVIRONMENT=prod \
            --dart-define=CONTENT_BASE_URL=https://example.com \
            --dart-define=AUTH_URL=https://example.com \
            --dart-define=EDIT_STATS_URL=https://example.com \
            --dart-define=DONATION_BASE_URL=https://example.com \
            --dart-define=DONATION_TOKEN=benchmark \
            --dart-define=SUPERWALL_API_KEY=benchmark \
            --dart-define=PAYWALL_ENV=live \
            --dart-define=FACEBOOK_APP_ID=benchmark \
            --dart-define=FACEBOOK_CLIENT_TOKEN=benchmark \
            --dart-define=ENABLE_DEVICE_PREVIEW=false
    artifacts:
      - build/ios/iphoneos/*.app
